# AI-Powered Personal Health Coach

# Starting the server
Before beginning, open a connection in MongoDBCompass at localhost:27017.
To start the backend: from the project root, move into the /server/ folder with "cd server", and run "npm install".
Then, running "npm start" will open the Express server and connect to the MongoDB database. 
